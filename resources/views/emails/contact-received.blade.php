ホームページのお問い合わせフォームから、新しいお問い合わせが届きました。

──────────────────────────────
受付日時：{{ $contact->created_at?->format('Y年n月j日 H:i') }}
お名前　：{{ $contact->name }}{{ $contact->name_kana ? '（'.$contact->name_kana.'）' : '' }}
メール　：{{ $contact->email }}
電話番号：{{ $contact->tel ?: '（未入力）' }}
件名　　：{{ $contact->subject ?: '（未入力）' }}
──────────────────────────────

【お問い合わせ内容】
{{ $contact->body }}

──────────────────────────────

このメールにそのまま返信すると、お客様（{{ $contact->email }}）へ直接届きます。
管理画面から返信すると、やり取りの記録が残ります。

管理画面：{{ $adminUrl }}
